{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{ encore_entry_link_tags('admin') }}
{% endblock %}
{% block body %}
    <div id="app" class="accent-1">
        <h1>
            <img class="logo" src="{{ asset('build/logo.svg') }}" alt="">
            <span>Lunch Cherry</span>
        </h1>

        <div class="loading">
            <div class="spinner spinner-clock"></div>
        </div>
    </div>
    {# TODO assets/js/admin/App.vue needs element #user with dataset user_id, name ... But, better to do refactoring #}
{% endblock %}
{% block javascripts %}
    {{ encore_entry_script_tags('admin') }}
{% endblock %}
